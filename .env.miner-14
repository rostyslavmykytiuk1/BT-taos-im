# Per-miner overlay for ./run_miner.sh -P miner-14
# 2026-06-25: A/B arm — AdaptiveRouterV2 NO-IDLE (allow_idle=0): trade every book (maker default /
# taker if rebated), never idle — the top-miners' breadth thesis. vs miner-10 (idle-on) + V1 fleet.
# REVERT: AGENT_NAME=AdaptiveRouterAgent AGENT_PARAMS=lazy_load=1.
WALLET_NAME=sn79
HOTKEY_NAME=sn79-14
PM2_NAME=miner-14
AXON_PORT=8104
AGENT_PATH=/root/sn-79/agents
AGENT_NAME=AdaptiveRouterV2Agent
AGENT_PARAMS="lazy_load=1 allow_idle=0"
