# Per-miner overlay for ./run_miner.sh -P miner-31
# 2026-06-26: NEW miner (uid 64) — PureMakerNCAgent (NEVER-CUT experiment fork). Live A/B vs the
# tight-cut PureMaker fleet to test the loss-cube/never-cut thesis. Reduce-to-breakeven + hold for
# reversion; loss realized only on catastrophe (30-40bps) or the bagged-book cap (24).
WALLET_NAME=sn79
HOTKEY_NAME=sn79-31
PM2_NAME=miner-31
AXON_PORT=8124
AGENT_PATH=/root/sn-79/agents
AGENT_NAME=PureMakerNCAgent
AGENT_PARAMS=lazy_load=1
